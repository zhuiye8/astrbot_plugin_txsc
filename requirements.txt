# AstrBot通用文生图插件依赖包
# 支持9个主流AI图像生成服务商

# 核心依赖
aiofiles>=23.1.0
httpx>=0.25.0
Pillow>=10.0.0

# 阿里云通义万相
dashscope>=1.22.1

# 字节跳动火山引擎
volcengine>=1.0.0

# 百度智能云千帆
qianfan>=0.3.0

# 科大讯飞星火 (使用requests进行HTTP调用)
requests>=2.25.0

# 智谱清言
zhipuai>=2.0.0

# OpenAI达芬奇
openai>=1.0.0

# 谷歌双子座
google-generativeai>=0.3.0

# PPIO图像生成 (使用httpx进行HTTP调用)
# httpx已在核心依赖中包含

# Grok图像生成 (兼容OpenAI SDK)
# openai已在上方包含